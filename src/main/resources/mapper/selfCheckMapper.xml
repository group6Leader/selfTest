<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 예약정보 처리 SQL -->
<mapper namespace="com.seltest.www.dao.SelfCheckMapper">

	<insert id="insertSelfCheck" parameterType="selfCheck">
		insert into selfCheck (
		
		selfCheck_Num
		, blood_Pressure
		, smoking
		, drinking
		, diabetes
		, surgery
		, disability
		, medicine
		, allergy
		, overseas_Visits
		, sleep_Time
		, stress
		, family_History
		, pregnant
		, cust_Num )
		
		values (
		
		selfCheck_Num_sequence.nextval
		, #{blood_Pressure}
		, #{smoking}
		, #{drinking}
		, #{diabetes}
		, #{surgery}
		, #{disability}
		, #{medicine}
		, #{allergy}
		, #{overseas_Visits}
		, #{sleep_Time}
		, #{stress}
		, #{family_History}
		, #{pregnant}
		, #{cust_Num} )
		
	</insert>
	
	<select id="readOne" resultType="selfCheck">
		select *
		from selfCheck
		where cust_Num = #{cust_Num}
	</select>
	
	
</mapper>
	
	